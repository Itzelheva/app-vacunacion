import { createSlice } from "@reduxjs/toolkit";



// ejemplo

{/*

constante (objeto) donde se van a colocar los valores iniciales de los estados

const initialState = {
  value: aqui va el o los valores de los estados iniciales,
}

export const DoseProgressSlice = createSlice({
  aqui se coloca la configuración del estado:
   name: "name"
   initialState
   reducers: aqui va la funcion encargada de avisarle al store como es que se va actualizar el estado
   dentro de reducers se colocan las funciones de como se van a actualizar esos estados
})


export const { exportar las acciones de reducers } = DoseProgressSlice.action

export default DoseProgressSlice.reducer




*/}